# One-time migration helper: copied controllers from legacy MVC into ShopOn.Web.
# The old MYBUSINESS project folder has been removed from this repo; use git history if you need the original sources.
$ErrorActionPreference = "Stop"
throw "port-controllers.ps1: legacy MYBUSINESS tree is no longer in the repo. Restore an old commit if you need to re-run this script."
